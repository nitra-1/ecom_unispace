export const metadata = {
  title: 'My Appointments | Aparna',
  description: 'View and manage your Aparna showroom appointments.',
}

export default function UserAppointmentsLayout({ children }) {
  return <section className="user-appointments-root">{children}</section>
}
