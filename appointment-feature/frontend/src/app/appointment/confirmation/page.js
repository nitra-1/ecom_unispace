import AppointmentConfirmed from './(component)/AppointmentConfirmed';

export const metadata = {
  title: 'Booking Confirmed – Aparna',
  description: 'Your consultation appointment has been confirmed.',
};

export default function ConfirmationPage() {
  return <AppointmentConfirmed />;
}
